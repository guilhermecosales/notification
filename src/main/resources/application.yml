spring:
  application:
    name: notification
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  mvc:
    problemdetails:
      enabled: true
  jpa:
    open-in-view: false

server:
  servlet:
    context-path: /notification